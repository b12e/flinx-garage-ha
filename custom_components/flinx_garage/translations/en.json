{
  "config": {
    "step": {
      "user": {
        "title": "F-LINX Garage Door",
        "description": "Enter your F-LINX cloud credentials. Real-time state is delivered via MQTT; commands are sent locally over Bluetooth when a door's opener is reachable.",
        "data": {
          "username": "Email",
          "password": "Password"
        }
      },
      "devices": {
        "title": "Select garage doors",
        "description": "All doors on your account are selected by default — uncheck any you don't want to add. You can change this later in the integration options.",
        "data": {
          "devices": "Devices"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to F-LINX API",
      "invalid_auth": "Invalid username or password",
      "no_devices": "No devices found on this account",
      "no_devices_selected": "Select at least one device",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "Account is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "F-LINX Garage Door options",
        "description": "Choose what to configure.",
        "menu_options": {
          "poll_interval": "Periodic cloud poll",
          "devices": "Manage devices",
          "bluetooth": "Bluetooth"
        }
      },
      "poll_interval": {
        "title": "Periodic cloud poll",
        "description": "MQTT push is the primary state source. Optionally poll the cloud API on a fixed schedule to recover state that MQTT silently drops (e.g. when the door is operated by a physical remote).",
        "data": {
          "poll_interval": "Periodic cloud poll"
        }
      },
      "devices": {
        "title": "Manage devices",
        "description": "Select which doors on this account to configure. Unchecking a door removes its device and entities from Home Assistant.",
        "data": {
          "devices": "Devices"
        }
      },
      "bluetooth": {
        "title": "Bluetooth",
        "description": "Commands are sent over Bluetooth first and fall back to the cloud. A Bluetooth advertisement doesn't say which door it belongs to, so with more than one door each has to be pointed at its own opener — otherwise that door stays cloud-only. Pick the opener for each door below."
      }
    },
    "error": {
      "no_devices_selected": "Select at least one device"
    },
    "abort": {
      "invalid_auth": "Invalid username or password",
      "no_devices": "No devices found on this account",
      "cannot_connect": "Failed to connect to F-LINX API"
    }
  },
  "services": {
    "refresh_state": {
      "name": "Refresh state",
      "description": "Force an immediate state read from the F-LINX cloud API, bypassing MQTT. Useful in automations when MQTT push updates are unreliable."
    }
  }
}
